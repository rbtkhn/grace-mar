# GOOD-MORNING — companion-xavier (`fork_id`: xavier)

**Prerequisite:** **Session 0** complete — [seed-survey-initiation.md](companion-xavier/docs/seed-survey/seed-survey-initiation.md) (all MCQs answered and saved). **Do not** start the ten beginner tasks until Session 0 is done.

## Daily rhythm (shape)

Mirror grace-mar **daily-warmup** cadence in *shape* — edit for Xavier’s lanes (companion-self build + EA/SMM client work when applicable):

1. Optional: `python3 scripts/operator_daily_warmup.py -u xavier` (if bundled in template).
2. `python3 scripts/harness_warmup.py -u xavier` — gate / session tail snapshot.
3. `python3 scripts/validate-integrity.py --user xavier --json` — review issues.
4. Skim **work-politics** content queue / client block if active — [work-politics README](../work-politics/README.md).

**Distinguish:** “Good morning” here = **her** Record + **her** work stack. Massie/client material is **WORK**, not SELF.

## First good morning — ten beginner tasks

**Prerequisite:** Session 0 (MCQ) **complete.**

| # | Task |
|---|------|
| 1 | Open the grace-mar repo in Cursor; navigate to `docs/skill-work/work-xavier/companion-xavier/`. |
| 2 | Open `companion-xavier/README.md`; read top to bottom. |
| 3 | Open `users/xavier/self.md` (repo root if symlinked). Skim — **IX empty** until gate merges survey outputs. |
| 4 | Command palette → Terminal → `pwd` / `ls` (or `dir` on Windows). |
| 5 | `python3 --version` — if missing, note for companion before installing runtimes. |
| 6 | Quick open `users/xavier/recursion-gate.md` — read **Candidates** header. |
| 7 | Open [LANES.md](LANES.md) — WORK vs Record once. |
| 8 | Ask the AI to summarize [AGENTS.md](../../../AGENTS.md): what may the agent do without approval vs what requires approval? |
| 9 | Optional: run harness + warmup from repo root; paste output to a scratch note. |
| 10 | Add one line to `users/xavier/session-log.md`: date + “Session 0 complete; first good morning tasks 1–10 done.” |

**Operator:** Pair first run with a short screen share if steps 4–6 stall.
