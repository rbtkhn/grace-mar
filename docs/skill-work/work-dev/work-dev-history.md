# work-dev-history — operator log

> **Append-only** log for the **work-dev** territory (OpenClaw, integration, exports, harness, transcripts). **Not** Record truth; **not** companion [self-memory](../../../users/grace-mar/self-memory.md). **Rotatable** — prune older sections when the file grows.

**Distinct from:** [work-dev-sources.md](work-dev-sources.md) (authorized sources only). **Operator rhythm:** [coffee](../../../.cursor/skills/coffee/SKILL.md) (**`coffee`**; legacy **`hey`** still works). **Per-lane log:** this file — [work-modules-history-principle.md](../work-modules-history-principle.md).

## How to append

- Use **`## YYYY-MM-DD`**; **`### Session 2`** (or UTC time) if multiple blocks the same day.
- **Scope:** integration steps, digest ingests, staging/merge protocol notes, CI/harness changes, cross-repo handbacks — **this lane only**.
- **Prefer:** one line per artifact (path, commit SHA, brief outcome).

## Log

_(Append below this line.)_

- **2026-04-06** — Positioning doc from external six-layer agent-infrastructure analysis: `positioning-governed-state-os.md` — "governed state OS" framing, companion-first vs infrastructure-first fork, gap map, accepted framings; README contents row added. Three actionable gaps added to `known-gaps.md`: GAP-008 (capability contracts), GAP-009 (compute ledger per-task cost), GAP-010 (sandbox adapter layer); triage note with six-layer priority order.
- **2026-04-06** — GAP-008 closed: capability contract template (`control-plane/capability-contract-template.yaml`) + two retrofits (`capability-contract-openclaw-export.yaml`, `capability-contract-openclaw-stage.yaml`) + `--validate-contracts` CLI wired into `agent_surface_checklist.py`. README index rows added. GAP-008 status → `implemented`.
- **2026-04-06** — GAP-009 closed: `emit_compute_ledger.py` extended with `task_id`, `task_type`, `outcome_confidence` (clamped 0–1, optional); `scripts/compute_ledger_summary.py` rollup (group by bucket/operation/task_type/task_id/date/model, `--since`, `--json`); three new tests. `economic-benchmarks.md` updated. GAP-009 status → `implemented`.
- **2026-04-06** — GAP-010 closed: `sandbox-adapter-spec.md` (spec) + `scripts/work_dev/sandbox_adapter.py` (adapter core + DryRunBackend + LocalDockerBackend stub). Authority check, pre/post pipeline receipts, compute-ledger row, backend registry. CLI: `sandbox_adapter.py --backend dry_run --command "…"`. 15 tests (`tests/test_sandbox_adapter.py`). GAP-010 status → `implemented`.
- **2026-04-06** — Wired `task_type` into all four `append_integration_ledger` callers: `openclaw_hook.py` (export), `openclaw_stage.py` (stage), `handback_server.py` (handback), `export_runtime_bundle.py` (export). New ledger rows carry task context for `--by task_type` summary grouping.
- **2026-04-06** — Capability contracts for sandbox backends: `capability-contract-sandbox-dry-run.yaml` (active), `capability-contract-sandbox-docker.yaml` (planned). Updated openclaw-export and openclaw-stage contracts with `task_type` receipt info. All 4 contracts validate.
- **2026-04-06** — Unit economics one-pager (`unit-economics-one-pager.md`) from live compute-ledger data: ~$0.25 total LLM cost over 45 days, ~$0.01/message, scaling constraint is operator labor not compute.
- **2026-04-06** — GAP-011: Record Diff Queue — unified review surface for pending governed-state changes. Extended `identity-diff.v1.json` with `recommendedAction` and `whyItMatters` (non-breaking). New `render_record_diff_queue.py` (template-portable queue renderer, sorts by confidence delta), `gate_to_diff_adapter.py` (recursion-gate → identity-diff v1 JSON), Flask `/diff-queue` + `/api/diff-queue` endpoints in `gate-review-app.py`. Demo data extended (`diff-002.json` with conflict note). 24 tests. All portable pieces upstreamed to companion-self; `template-manifest.json` updated in both repos.
- **2026-04-07** — Capability shift detector: `detect_capability_shift.py` (6 providers — Anthropic, OpenAI, xAI/Grok, Google/Gemini, DeepSeek, Qwen), `capability-assumptions.yaml` (12 seed assumptions), warmup one-liner, gate-review-pass pre-check reference. GAP-015 closed. Both repos (grace-mar + companion-self for portable core).
- **2026-04-07** — Arbitrage rotation framework ingest (Polymarket bot transcript): LIB-0150 entry, rolling-disruption change-review trigger, architecture design principle, discipline note, three diagnostic scripts (staleness, rhythm, warrant), pipeline wiring (gate-review, warmup, suggest-candidates), GAP-012/013/014.
- **2026-04-04** — Agent memory PostgreSQL/pgvector spec and v1 migration: `agent-memory-pgvector-spec.md`, `sql/agent_memory_v1_initial.sql`; README contents row (flaw-fix plan implementation).
- **2026-04-07** — Evidence search (`search_evidence.py`): TF-IDF + cosine similarity over `self-archive.md` entries. Stdlib-only, zero dependencies. 186 entries indexed (ACT, WRITE, CREATE, MEDIA, GATE). Origin: MemPalace spatial-indexing proposal (5 integration patterns, ChromaDB + SQLite, new directory tree) → redundancy filter collapsed 4/5 patterns against existing architecture (dream contradiction digest, IX-A/B/C categories, RECURSION-GATE governance, deferred AutoGen/ASSUME-013) → disproportion named (heavyweight machinery for one actual gap: retrieval degrades as Record grows) → minimum extraction (single script, TF-IDF, no persistent index). Compression-as-discovery: the proposal was raw material; each round removed structure and revealed the real need — the Record must be *findable*, not re-organized. Same pattern as capability-shift-detector earlier this session (external concept → evaluate redundancy → extract one automated sensor → single script). Skill candidate logged.
- **2026-04-07** — Dream integration for capability shift detector: `auto_dream.py` now runs live model-category fetch during the maintenance pass (`offline=False`, `category="model"`). Results written to `last-dream.json` handoff (capability_shift block) and text summary. REVIEW alerts surface in followups for morning coffee. Cadence: dream does the live scan → caches → warmup shows cached line next morning. Dream SKILL.md updated (return brief, examples, related files).
- **2026-04-07** — Steward pass: refreshed 6 stale derived exports (manifest, llms.txt, intent_snapshot, fork-manifest, PRP, runtime-bundle). Synced ASSUME-007 keyword refinement (narrow cross-session phrases replacing broad agent/tool terms to reduce false positives) from grace-mar to companion-self. Integrity restored to PASS.
- **2026-04-07** — OB1-inspired Record enhancements (4 scripts, both repos): `batch_ingest_observations.py` (multi-observation → gate candidates in one pass), `detect_capture_gap.py` (Evidence staleness sensor, 7/14/30d thresholds), `assess_session_load.py` (cognitive load → annotated coffee A–E menu), `weekly_record_digest.py` (automated Record growth summary). Wired into dream (capture_gap summary + followup), warmup (gap_line one-liner), coffee (load assessment after Step 1, SKILL.md Step 2 load-annotated menu). All 4 scripts copied to companion-self with auto-detection for repo structure differences (self-archive.md vs self-evidence.md, unified vs separate IX files). Companion-self wire-ups: good-night-brief.py (capture_gap in handoff), good-morning-brief.py (gap + load lines), cadence-coffee.py (load one-liner). Origin: OB1 community member architecture post — multi-capture ingest, pg_cron backfill pattern, weekly-review query, decomposition-with-approval. Each idea filtered for redundancy, compressed to the actual gap it fills.
- **2026-04-07** — Scion convergence analysis (GoogleCloudPlatform/scion): multi-agent orchestration testbed — container-isolated agents, git worktrees, markdown-defined behavior, template → instance pattern, skills as modular surfaces. Tangential surfaces to grace-mar validated: template-to-instance (companion-self → grace-mar), skills-as-modules (.cursor/skills/), markdown-as-governance (AGENTS.md). Scion added to capability-assumptions.yaml ecosystem sources; ASSUME-013 notes updated (container-based isolation as alternative axis to framework dependencies).
- **2026-03-30** — Added Huang / Lex #494 digest and OpenClaw/Grace-Mar diff: `research/external/work-dev/transcripts/lex-fridman-494-jensen-huang-DIGEST.md`; README index row; commits through `51de012` (operator thread).
- **2026-04-07** — `work-dev-teach.md`: upgraded OB1 paste-in prompt — working rules (repo-grounded conclusions, path-drift handling), Step 1 requires citations per mechanism, governed-learning definition + evidence-shaped/staged/reviewed/mergeable criterion, Step 3 pipeline table with unambiguous Merge row, Step 7 expanded to five outputs (gap map, honest uncertainty list).
- **2026-04-08** — Proving Ground convergence + Judgment Probe Harness: external benchmark ([proving-ground](https://github.com/petersimmons1972/proving-ground)) found Judgment +2.1pt for profiled vs blank agents, Creativity +1.0pt, Correctness flat. Validates governed identity as design choice. Extracted two high-leverage pieces: (1) `run_judgment_probes.py` — 8 age-appropriate probes targeting IX-C personality tensions (persistence vs help-seeking, jump-in vs observe, resistance vs structure, bravery vs safety, quiet hurt vs speaking up, attention drift, recognition seeking, recovery under contradiction); deterministic four-dimension scoring (committed, trait_aligned, tension_preserved, age_appropriate). (2) `eval_identity_delta.py` — before/after comparison for gate merges; runs judgment + voice benchmarks, computes per-dimension deltas against saved baseline. No LLM-as-judge; no optimization framing (measures, does not prescribe). Instance-doctrine success metrics table updated.
- **2026-04-09** — Daily brief operator hygiene: §1e explicit “no weak signal over threshold” closures + one-line rationale on `daily-brief-2026-04-03.md`, `daily-brief-2026-04-07.md`, `daily-brief-2026-04-09.md`; §1d filled for 2026-04-09 (Kremlin Ivanovo meeting + Arctic Forum schedule, env fetch caveat); `daily-brief-config.json` `_comment` documents Reuters `worldNews` RSS intermittent failure and tier-1 feed coverage. Commit `fa3bc6c`. (WORK-strategy artifacts; operator discipline / doc quality.)
